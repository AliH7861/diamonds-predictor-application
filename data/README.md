# Data

`raw/` contains source data and download markers. Raw datasets are inputs and must
not be edited by preprocessing code. `processed/classification/` and
`processed/regression/` contain reproducible generated datasets and statistics.
Do not place models, figures, or notebooks here.
